[project]
name = "cloud_storage_api"
version = "1.0.0"
description = "Abstract cloud storage client base class (ABC)"
requires-python = ">=3.12"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["cloud_storage_api"]
